<%= .find(session[:user]).name %>さんの投稿

	<% @tasks.each do |task| %>
		<p><%= t.artist %></p>
		<p><%= t.album %></p>
		<p><%= t.track %></p>
		<p><a href="<%= t.sampleurl %>">サンプル</p>
		<%=CGI.escapeHTML(task.comment) %>
		<% end %>
	<% end %>
<% end %>